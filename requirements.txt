# Base requirements
torch --index-url https://download.pytorch.org/whl/cu121
transformers>=4.35.0
accelerate
datasets[audio]

# For audio processing (if not fully covered by datasets[audio] or system ffmpeg)
# librosa

# For performance enhancements
# Flash Attention 2 (requires compatible GPU and CUDA toolkit >= 11.6)
flash-attn --no-build-isolation

# Optimum for BetterTransformer
optimum

# For serving the model via HTTP (recommended for RunPod)
fastapi
uvicorn[standard]
python-multipart # For file uploads with FastAPI
